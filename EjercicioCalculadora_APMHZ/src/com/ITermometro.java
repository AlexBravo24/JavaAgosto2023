package com;

public interface ITermometro {

	// Las interfaces funcionan como un plano
	// o "Blueprint" que va a contener una
	// colección de métodos abstractos
	// para ser implementados por otra clase
	// Puede contener también en caso necesario
	// declaradas variables "constantes" (static final)
	
	public void tomarTemperatura(); // no hay necesidad de agragar la 
	// palabra reservada abstract porque una interface solo acpeta
	// métodos abstractos
	
	
	
}
